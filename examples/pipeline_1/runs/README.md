# Run Output Directory

In this directory is the outputs of the example pipeline. This directory's contents are untracked if the contents are further directories. This means there are no example pipeline outputs!

However, you can run pipeline_1 to generate its outputs yourself. Simply type the following command into a powershell or cmd terminal:
```powershell
python examples/pipeline_1/main.py
```